import React from "react";
// Functional Component
const Anything = props =>{
return <h1> Hello Sarfraz {props.children}</h1>
}
export default Anything;
// same we can use props.children in class component